version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  compiler:
    build: ./compiler
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
